//Prompt: Initialize Two Variables, Add Them and Print the Result in C++

#include <iostream>

int main() {

    int a = 10;
    int b = 20;
    int sum = a + b;
    std::cout << "Sum of " << a << " and " << b << " is: " << sum << std::endl;

    return 0;
}

/*
    Notes:
        - Variables 'a' and 'b' are declared and initialized with integer values.
        - The sum of 'a' and 'b' is calculated and stored in the variable 'sum'.
        - The result is printed to the console using std::cout.
*/